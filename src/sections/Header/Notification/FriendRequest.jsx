import { MenuItem } from '@mui/material';

export const FriendRequest = ({ notification }) => {
	const { payload, notificationType, createdAt, isNew } = notification;
	return <MenuItem></MenuItem>;
};
